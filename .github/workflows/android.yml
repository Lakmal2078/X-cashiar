<<<<<<< Updated upstream
name: "Android CI Build & APK Artifacts"
=======
name: Android CI (Capacitor)
>>>>>>> Stashed changes

on:
  push:
    branches: [ 'fix/android-capacitor' ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      JAVA_HOME_11_X64: /opt/hostedtoolcache/jdk/11/x64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Install dependencies
        run: npm ci

<<<<<<< Updated upstream
      - name: "Locate Android Project & Prepare Environment"
=======
      - name: Build web assets
        run: npm run build:web

      - name: Install Capacitor packages
        run: npm install --no-save @capacitor/cli @capacitor/android @capacitor/core

      - name: Add or sync Android platform
        run: |
          npx cap add android || true
          npx cap sync android

      - name: Set up Android SDK
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 33
          target: default
          arch: x86_64

      - name: Build Android debug APK (Gradle)
        working-directory: android
>>>>>>> Stashed changes
        run: |
          chmod +x ./gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk

